name: DeepTrainer

channels:
  - pytorch
  - conda-forge
  - defaults
  - nvidia

dependencies:
  - python=3.11
  - pytorch
  - torchvision
  - pytorch-cuda=12.4
  - lightning
  - torchmetrics

  - pip>=23
  - pip:
      - hydra-optuna-sweeper
      - hydra-colorlog
      - rootutils
      - torch-summary
      - timm
      - onnx
      - onnxruntime-gpu
      - torchinfo
      - hydra-core
      - rich
      - pre-commit
      - pytest
      - wandb
      - opencv-python
      - matplotlib
      - scikit-learn
      - pynvml
      - ruff
      - segmentation-models-pytorch
      - ipywidgets
      - ipykernel
      - pandas
      - tensorrt
      - pycuda