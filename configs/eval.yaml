# @package _global_

defaults:
  - _self_
  - data: mnist # choose datamodule with `test_dataloader()` for evaluation
  - model: mnist
  - logger: null
  - trainer: default
  - paths: default
  - extras: default
  - hydra: default
  - callbacks: rich_progress_bar

  # testing runs
  - runs: null

task_name: "eval" # eval or predict

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: " " 
